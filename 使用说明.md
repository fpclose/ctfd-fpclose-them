# CTFd-FPClose 主题使用说明

## 项目信息

- **主题名称**: CTFd-FPClose
- **版本**: v1.0
- **支持 CTFd 版本**: v3.8.1 及以上
- **GitHub 仓库**: https://github.com/fpclose/ctfd-fpclose-them
- **发布日期**: 2024-11-22

---

## 主题特性

### 🌊 动态 Pipeline 背景
这个主题的核心特性是在挑战页面顶部的动态管道背景动画：
- 基于 HTML5 Canvas 技术
- 使用 Simplex Noise 算法实现自然流动效果
- 30 个动态管道线条
- 蓝绿色发光效果
- 硬件加速，性能优化

### ⭐ 星空背景
页面背景具有深邃的星空夜景效果：
- 50+ 颗不同大小和亮度的星星
- 星云效果叠加
- 自然的闪烁动画
- 深蓝色渐变背景

### 🎨 现代化设计
- 深色主题优化
- 毛玻璃效果导航栏
- 优雅的颜色配色
- 挑战卡片光效
- 完全响应式设计

---

## 快速安装

### 方法 1: 从 GitHub 克隆（推荐）

```bash
# 进入 CTFd 主题目录
cd /path/to/your/CTFd/CTFd/themes/

# 克隆主题仓库
git clone https://github.com/fpclose/ctfd-fpclose-them.git ctfd-fpclose

# 重启 CTFd
docker compose restart ctfd
```

### 方法 2: 手动下载

1. 访问 https://github.com/fpclose/ctfd-fpclose-them
2. 点击 "Code" → "Download ZIP"
3. 解压到 CTFd 主题目录：`/path/to/CTFd/CTFd/themes/ctfd-fpclose`
4. 重启 CTFd

---

## 激活主题

### 通过管理面板激活

1. 登录 CTFd 管理员账号
2. 访问 **Admin Panel** → **Config** → **Appearance**
3. 在 **Theme** 下拉菜单中选择 **ctfd-fpclose**
4. 点击 **Update** 保存
5. 访问挑战页面验证效果

### 通过环境变量激活

在 `docker-compose.yml` 中添加：
```yaml
services:
  ctfd:
    environment:
      - CTFD_THEME=ctfd-fpclose
```

然后重启容器：
```bash
docker compose restart ctfd
```

---

## 文件说明

### 核心文件

#### Pipeline 动画相关
- `static/js/pipeline/pipeline-jumbotron.js` - Pipeline 动画主逻辑
- `static/js/pipeline/util.js` - 工具函数（数学计算）
- `static/js/pipeline/noise.min.js` - Simplex Noise 库
- `static/css/pipeline/pipeline-jumbotron.css` - Pipeline 样式

#### 星空背景相关
- `static/css/starry-background.css` - 星空背景效果

#### 其他样式
- `static/css/challenge-card-light.css` - 挑战卡片光效样式

#### 模板文件
- `templates/base.html` - 基础模板（包含所有资源引用）
- `templates/challenges.html` - 挑战页面（应用 Pipeline 背景）
- 其他 templates/ 下的文件 - 各个页面模板

### 文档文件

- `README.md` - 中文主说明文档
- `README_EN_OLD.md` - 英文说明文档
- `INSTALL_CN.md` - 详细安装指南
- `THEME_DOCUMENTATION.md` - 主题技术文档
- `QUICK_START.md` - 快速开始指南
- `使用说明.md` - 本文件

---

## 自定义配置

### 修改 Pipeline 动画参数

如果你想调整动画效果，可以编辑 `static/js/pipeline/pipeline-jumbotron.js`：

```javascript
// 管道数量（默认 30）
const pipeCount = 30;

// 速度设置
const baseSpeed = 0.5;    // 基础速度
const rangeSpeed = 1;     // 速度随机范围

// 颜色设置（HSL 色相）
const baseHue = 180;      // 基础色相（180=青色，120=绿色，240=蓝色）
const rangeHue = 60;      // 色相变化范围

// 背景颜色
const backgroundColor = 'hsla(150,80%,1%,1)';

// 线条粗细
const baseWidth = 2;      // 基础宽度
const rangeWidth = 4;     // 宽度随机范围
```

### 调整性能（针对低配设备）

如果动画在低性能设备上卡顿，可以：

1. **减少管道数量**：
   ```javascript
   const pipeCount = 15;  // 默认 30
   ```

2. **降低速度**：
   ```javascript
   const baseSpeed = 0.3;  // 默认 0.5
   ```

3. **禁用模糊效果**：
   在 `render()` 函数中注释掉：
   ```javascript
   // ctx.b.filter = 'blur(12px)'
   ```

### 应用到其他页面

要在其他页面也显示 Pipeline 背景，编辑相应的模板文件，添加：

```html
<div class="jumbotron pipeline-jumbotron">
  <div class="container">
    <h1>你的标题</h1>
  </div>
</div>
```

JavaScript 会自动检测并初始化动画。

---

## 兼容性

### CTFd 版本
- ✅ **CTFd 3.8.1** - 完全兼容（推荐）
- ✅ **CTFd 3.8.x** - 兼容
- ⚠️ **CTFd 3.7.x** - 可能兼容，需测试
- ❌ **CTFd 3.6.x 及以下** - 不兼容

### 浏览器兼容性
- ✅ Chrome/Edge 90+
- ✅ Firefox 88+
- ✅ Safari 14+
- ✅ 移动浏览器（iOS Safari, Chrome Mobile）
- ❌ IE 11 及以下（不支持 Canvas API）

---

## 常见问题

### Q1: Pipeline 动画不显示？

**检查步骤**：
1. 打开浏览器开发者工具（F12）
2. 查看 Console 标签页是否有错误
3. 查看 Network 标签页，确认 JS/CSS 文件已加载
4. 确认页面有 `<div class="jumbotron pipeline-jumbotron">` 元素

### Q2: 主题无法激活？

**解决方法**：
1. 确认文件夹名称为 `ctfd-fpclose`（不能有其他名称）
2. 确认 `templates/base.html` 文件存在
3. 重启 CTFd 服务
4. 清除浏览器缓存

### Q3: 动画卡顿怎么办？

**优化方法**：
- 降低 `pipeCount` 参数
- 减小 `baseSpeed` 参数
- 禁用模糊效果
- 升级硬件或浏览器

### Q4: 如何更改主题颜色？

**修改位置**：
1. Pipeline 动画颜色：修改 `pipeline-jumbotron.js` 中的 `baseHue` 和 `rangeHue`
2. 标题颜色：修改 `pipeline-jumbotron.css` 中的 `.pipeline-jumbotron h1` 样式
3. 星空颜色：修改 `starry-background.css` 中的背景渐变

---

## 开发与修改

如果你想修改主题源代码并重新构建：

### 环境要求
- Node.js 16+
- Yarn 1.x

### 安装依赖
```bash
cd ctfd-fpclose
yarn install
```

### 开发模式（自动监听和编译）
```bash
yarn dev
```

### 生产构建
```bash
yarn build
```

### 代码格式化
```bash
yarn format
```

---

## 技术栈

- **前端框架**: Bootstrap 5.3
- **JavaScript 库**: Alpine.js, Vue.js 3
- **构建工具**: Vite 3
- **样式预处理**: SCSS/Sass
- **图标**: Font Awesome 6
- **字体**: Lato, Raleway
- **动画**: HTML5 Canvas, Simplex Noise

---

## 许可证

本主题基于 MIT 许可证开源。

---

## 致谢

- **CTFd Team** - 优秀的 CTF 平台
- **Sean Free** - Pipeline 动画原作者
- **jwagner** - Simplex Noise 库作者

---

## 联系与支持

- **GitHub 仓库**: https://github.com/fpclose/ctfd-fpclose-them
- **问题反馈**: https://github.com/fpclose/ctfd-fpclose-them/issues

---

## 版本历史

### v1.0 (2024-11-22)
- ✨ 初始版本发布
- 🌊 Pipeline 动画背景
- ⭐ 星空背景效果
- 🎨 现代化深色主题
- 📱 完全响应式设计
- ✅ 支持 CTFd 3.8.1

---

**享受你的新主题！** 🎉

如有问题，请查阅 `INSTALL_CN.md` 或 `THEME_DOCUMENTATION.md` 获取更详细的信息。
